int open_client_socket(char * host, int port);
int sendCommand(char *  host, int port, char * command, char * response) ;
int sendCommand_slient (char *  host, int port, char * command, char * response);
void printUsage();
//int main(int argc, char **argv);

